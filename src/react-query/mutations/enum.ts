export const enum AUTH_MUTATION_KEYS {
  LOGIN = 'login',
  SIGN_UP = 'register',
}

export const enum PROFILE_MUTATION_KEYS {
  LOGOUT = 'logout',
}

export const enum BLOG_MUTATION_KEYS {
  ADD_BLOG = 'addBlog',
}
